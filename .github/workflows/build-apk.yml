name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: [actions/setup-java@v4](https://github.com)
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle' # 新增快取功能以加速後續建置

      - name: Setup Gradle
        uses: [gradle/actions/setup-gradle@v3](https://github.com)

      - name: 🚑 自動修復 Gradle 權限與格式
        run: |
          # 解決 Windows/Linux 換行符號相容性與執行權限問題
          sed -i 's/\r$//' gradlew
          chmod +x gradlew

      - name: Build APK (Debug)
        # 使用 ./gradlew 確保環境一致
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: [actions/upload-artifact@v4](https://github.com)
        with:
          name: VR-Player-App
          path: app/build/outputs/apk/debug/*.apk
          
