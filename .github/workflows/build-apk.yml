name: Debug Build

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: 🔍 檢查檔案位置 (偵探模式)
        run: |
          echo "=== 正在檢查你的檔案有沒有放對房間... ==="
          find . -maxdepth 5 -not -path '*/.*'
          echo "=========================================="

      - name: Build APK (使用系統 Gradle)
        # 這裡改用 gradle 直接指令，避開 gradlew 權限問題
        run: gradle assembleDebug --stacktrace

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: VR-Player-App
          path: "**/*.apk"
