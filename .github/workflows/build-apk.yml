            - 
          
name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    name: Android Build
  # You may pin to the exact commit or the version.
  # uses: vgaidarji/android-github-actions-build@0888eff41ad409a802aa4ccd7790a91891da9056
  uses: vgaidarji/android-github-actions-build@v1.0.1
  
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: 🚑 自動修復 Gradle 權限與格式
        run: |
          # 1. 移除 Windows 換行符號 (解決 Pipe call failed)
          sed -i 's/\r$//' gradlew
          # 2. 給予執行權限 (解決 Permission denied)
          chmod +x gradlew

      - name: Build APK (強制使用 Wrapper)
        # 使用 ./gradlew 確保版本一致，並加上 --stacktrace 查看詳細錯誤
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: VR-Player-App
          path: app/build/outputs/apk/debug/*.apk
